# Python dependencies bundled into the Lambda layer (cdk/stacks/api_stack.py).
# Installed at deploy time via CDK Docker bundling into python/ (see LayerVersion).
# boto3 is provided by the Lambda runtime — do not list it here to avoid duplicate copies.

hubspot-api-client>=9.0.0
stripe>=8.0.0
anthropic>=0.40.0
requests>=2.31.0
