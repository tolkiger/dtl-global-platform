# DTL-Global Platform — Local Development & Testing Dependencies
#
# Install: pip install -r requirements.txt
#
# Lambda runtime deps match cdk/lambda_layer/requirements.txt (built into a Lambda layer by CDK).
# boto3 is included here for local scripts/tests; Lambda uses the runtime boto3.

# --- Same pins as Lambda layer (cdk/lambda_layer/requirements.txt) ---
-r cdk/lambda_layer/requirements.txt

# --- AWS (local / tests; Lambda runtime supplies boto3) ---
boto3>=1.34.0

# --- CDK ---
aws-cdk-lib>=2.180.0
constructs>=10.4.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0
moto>=5.0.0

# --- Development ---
python-dotenv>=1.0.0
