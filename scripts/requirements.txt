# Phase 0 setup scripts — install with:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r scripts/requirements.txt
# HubSpot calls use Python stdlib (urllib); Stripe uses the official SDK.
stripe>=8.0.0
boto3>=1.34.0